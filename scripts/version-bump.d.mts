/**
 * Type declaration for the plain-JavaScript release helper, so the test can
 * import it without the type-aware lint rules losing track of what it returns.
 */
export declare function nextVersion(from: string, bump: string): string;
