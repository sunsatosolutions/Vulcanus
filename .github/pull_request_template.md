## What this changes

<!-- One or two sentences on what the CLI does differently now. -->

## Why

<!-- The problem it solves. Link an issue if there is one. -->

## Checks

- [ ] `npm run typecheck`, `npm run lint`, `npm run format`, `npm test` all pass
- [ ] A freshly generated vault still passes `vulcanus doctor`
- [ ] User-visible changes are described in `CHANGELOG.md` under `## Unreleased`
- [ ] No change makes a command claim something it did not do

## Anything a reviewer should look at closely

<!-- Trade-offs, things you were unsure about, parts you would like a second opinion on. -->
